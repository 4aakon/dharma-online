import React from "react";
import ReactDOM from "react-dom/client";
import DharmaOnline from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <DharmaOnline />
  </React.StrictMode>
);
